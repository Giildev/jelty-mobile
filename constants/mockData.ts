/**
 * This file previously contained mock data used during development.
 * All mock data has been removed, and the application now fetches real data from the backend.
 * Keep this file empty or remove it after cleaning up all references.
 */

export const MOCK_MEALS = [];
export const MOCK_EXERCISES = [];
export const MOCK_PROGRESS = null;
export const MOCK_STREAK = null;
export const MOCK_SCHEDULED_MEALS = [];
export const MOCK_WEEKLY_MEAL_PLAN = { weekStart: "", meals: [] };
export const MOCK_SCHEDULED_EXERCISES = [];
export const MOCK_WEEKLY_WORKOUT_PLAN = { weekStart: "", exercises: [] };
export const MOCK_GROCERY_ITEMS = [];
export const MEAL_DETAILS_MOCK = [];
export const EXERCISE_DETAILS_MOCK = [];
